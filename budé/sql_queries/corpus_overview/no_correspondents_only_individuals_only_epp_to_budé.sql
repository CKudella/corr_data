SELECT COUNT(DISTINCT name_in_edition) AS 'Total Number of individual Correspondents who wrote to Budé but did not receive letters by Budé' FROM correspondents WHERE correspondents.correspondents_id NOT IN (SELECT DISTINCT recipient_id FROM letters) and correspondents.type = 'individual'
