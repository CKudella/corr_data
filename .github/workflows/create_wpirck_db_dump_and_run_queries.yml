name: Pirckheimer - Setup MySQL, Import Data, Create DB Dump, and Run Queries

on:
  push:
    paths:
      - 'pirckheimer/dataset/csv/**'  # Only trigger if files in this directory change

concurrency:
  group: committing-workflows
  cancel-in-progress: false  # Ensures jobs wait instead of canceling the running one

jobs:
  setup-mysql:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: wpirck_cdb_v1
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --host=localhost --user=root --password=root"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
    
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Python and Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas mysql-connector-python

      - name: Run Python Script to Setup Database and Import Data
        run: python pirckheimer/database/create_script/import_data.py

      - name: Dump Database to SQL File
        run: |
          mysqldump -h 127.0.0.1 -u root -proot wpirck_cdb_v1 > pirckheimer/database/dump/wpirck_cdb_v1_dump.sql

      - name: Upload Database Dump as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: wpirck_cdb_v1_dump
          path: pirckheimer/database/dump/wpirck_cdb_v1_dump.sql

      - name: Run SQL Queries and Save Results  
        run: python pirckheimer/run_queries.py  

      - name: Upload Query Results as Artifact
        uses: actions/upload-artifact@v4  
        with:  
          name: query-results  
          path: pirckheimer/query_results/

#    - name: Configure Git
#      run: |
#        git config --global user.name "CKudella"
#        git config --global user.email "CKudella@users.noreply.github.com"
#
#    - name: Commit and push changes
#      run: |
#        git add -A
#        git commit -m "Auto-update db dump and query_results" || echo "No changes to commit"
#        git push https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/ckudella/corr_data.git HEAD:main
